@model LibraryBook.Models.Genre

@{
    ViewData["Title"] = "Details";
}

<h1>Детали</h1>

<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <link rel="stylesheet" href="~/css/Details.css"/>
    <a asp-action="Edit" class="knopka2" asp-route-id="@Model?.Id">Изменить</a> 
    <a class="knopka2" asp-action="Index">Назад</a>
</div>
